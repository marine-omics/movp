params {
        max_cpus=64
        max_memory=600.GB
        fb_chunksize=1000000        
}

process {
  scratch = true
  stageInMode = 'symlink'
  executor = 'local'
}

singularity {

        enabled = true
        autoMounts = true 
        runOptions='--bind /fast/tmp:/tmp'
}